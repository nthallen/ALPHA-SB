ICM20948 {
  HBox { +-; ICM20948; -+ };
  Max:   (ICM_max_g,7) g;
  Rate:    (ICM_sps,5) Hz;
  Rem:     (ICM_rem,5);
  msecs: (ICM_msecs,5) msecs;
  MLF:     (ICM_mlf,5);
  Mode:   (ICM_mode,3);
  Scale:    (ICM_fs,7) g;
  Gp:      (ICM_Gp,10);
  Gi:      (ICM_Gi,10);
  Stale: (ICM_stale,3);
}
